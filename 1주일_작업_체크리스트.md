# 크루즈 지니 - 1주일 작업 체크리스트

> **목표**: 출시 가능 수준 95% 달성  
> **기간**: 2025년 11월 4일 (월) ~ 11월 8일 (금)  
> **핵심**: 번역기 + 검색 강화 (50대+ 최적화)

---

## 📅 Day 1 (월요일) - 온보딩 수정

### ⏰ 오전 (3시간)

#### ✅ 작업: 예약번호 → 크루즈 선택 드롭다운

**파일**: `/components/SimplifiedOnboarding.tsx`

**체크리스트**:
```
□ 파일 열기: /components/SimplifiedOnboarding.tsx
□ 예약번호 입력 필드 찾기 및 삭제
□ 다음 코드 추가:

const [products, setProducts] = useState([]);
const [selectedProduct, setSelectedProduct] = useState('');

useEffect(() => {
  fetch('/api/cms/products')
    .then(res => res.json())
    .then(data => setProducts(data.products || []));
}, []);

<select
  value={selectedProduct}
  onChange={(e) => setSelectedProduct(e.target.value)}
  className="w-full p-4 text-xl border-2 rounded-lg"
>
  <option value="">크루즈를 선택하세요</option>
  {products.map(p => (
    <option key={p.productCode} value={p.productCode}>
      {p.packageName} ({p.nights}박 {p.days}일)
    </option>
  ))}
</select>

□ 기존 자동 생성 로직 확인 (변경 불필요)
□ 테스트: 브라우저에서 확인
□ 저장 및 커밋
```

**예상 소요**: 3시간

---

## 📅 Day 2 (화요일) - 번역기 대폭 개선 🔥

### ⏰ 오전 (4시간): 버튼 크기 & 빠른 문장

#### ✅ 작업 1: 버튼 크기 2배 증가 (2시간)

**파일**: `/app/translator/page.tsx`

**체크리스트**:
```
□ 파일 열기: /app/translator/page.tsx
□ 374-394줄 찾기 (BTN_PAIRS 버튼 부분)
□ className 수정:

// Before
className="... px-4 py-4 ..."

// After
className="
  w-full px-6 py-8 rounded-2xl text-xl font-bold
  shadow-xl min-h-[120px]
  bg-gradient-to-r from-blue-600 to-blue-500 text-white
  hover:from-blue-700 hover:to-blue-600
  active:scale-95 transition-all
"

// 내부 구조도 변경
<div className="flex flex-col items-center gap-3">
  <span className="text-5xl">
    {listening === 'recording' ? '🔴' : '🎤'}
  </span>
  <span className="text-xl">{p.label}</span>
  <span className="text-sm font-normal opacity-90">
    (버튼을 꾹 누르고 말하세요)
  </span>
</div>

□ 사진 찍기 버튼도 크게 (298-300줄):
className="px-6 py-4 text-xl font-bold bg-green-600 rounded-xl shadow-lg"

<FiCamera size={32} />
<span>📷 사진으로 번역</span>

□ 테스트: 버튼 터치하기 쉬운지 확인
□ 저장
```

**예상 소요**: 2시간

---

#### ✅ 작업 2: 빠른 문장 추가 (2시간)

**체크리스트**:
```
□ 같은 파일 (/app/translator/page.tsx)
□ 28줄 아래에 다음 코드 추가:

const QUICK_PHRASES = {
  'ja-JP': [
    { ko: '화장실이 어디에요?', ja: 'トイレはどこですか？', emoji: '🚻' },
    { ko: '얼마예요?', ja: 'いくらですか？', emoji: '💰' },
    { ko: '이거 주세요', ja: 'これをください', emoji: '🛒' },
    { ko: '맛있어요', ja: 'おいしいです', emoji: '😋' },
    { ko: '감사합니다', ja: 'ありがとうございます', emoji: '🙏' },
    { ko: '천천히 말해주세요', ja: 'ゆっくり話してください', emoji: '🗣️' },
    { ko: '사진 찍어도 되나요?', ja: '写真を撮ってもいいですか？', emoji: '📷' },
    { ko: '도와주세요', ja: '助けてください', emoji: '🆘' },
  ],
  'zh-CN': [
    { ko: '화장실이 어디에요?', cn: '厕所在哪里？', emoji: '🚻' },
    { ko: '얼마예요?', cn: '多少钱？', emoji: '💰' },
    { ko: '이거 주세요', cn: '我要这个', emoji: '🛒' },
    { ko: '맛있어요', cn: '好吃', emoji: '😋' },
    { ko: '감사합니다', cn: '谢谢', emoji: '🙏' },
    { ko: '천천히 말해주세요', cn: '请慢点说', emoji: '🗣️' },
    { ko: '사진 찍어도 되나요?', cn: '可以拍照吗？', emoji: '📷' },
    { ko: '도와주세요', cn: '请帮帮我', emoji: '🆘' },
  ],
  'en-US': [
    { ko: '화장실이 어디에요?', en: 'Where is the bathroom?', emoji: '🚻' },
    { ko: '얼마예요?', en: 'How much is it?', emoji: '💰' },
    { ko: '이거 주세요', en: 'I\'ll take this', emoji: '🛒' },
    { ko: '맛있어요', en: 'It\'s delicious', emoji: '😋' },
    { ko: '감사합니다', en: 'Thank you', emoji: '🙏' },
    { ko: '천천히 말해주세요', en: 'Please speak slowly', emoji: '🗣️' },
    { ko: '사진 찍어도 되나요?', en: 'Can I take a photo?', emoji: '📷' },
    { ko: '도와주세요', en: 'Please help me', emoji: '🆘' },
  ],
};

□ 337줄 (대화 기록 표시 부분) 위에 빠른 문장 UI 추가:

{!isCruising && (
  <div className="mb-6 bg-gradient-to-r from-blue-50 to-purple-50 p-4 rounded-2xl">
    <h3 className="text-xl font-bold mb-3 flex items-center gap-2">
      <span>⚡</span>
      <span>자주 쓰는 문장</span>
      <span className="text-sm font-normal text-gray-600">(클릭하면 바로 번역)</span>
    </h3>
    <div className="grid grid-cols-2 gap-3">
      {(QUICK_PHRASES[localLang.code] || QUICK_PHRASES['en-US']).map((phrase, idx) => {
        const langKey = localLang.code === 'ja-JP' ? 'ja' 
                      : localLang.code.startsWith('zh') ? 'cn' 
                      : 'en';
        return (
          <button
            key={idx}
            onClick={() => {
              setItems(prev => [{
                id: Date.now().toString(),
                from: { flag: '🇰🇷', name: '한국어' },
                to: { flag: localLang.flag, name: localLang.name },
                source: phrase.ko,
                translated: phrase[langKey],
                when: new Date().toLocaleTimeString('ko-KR'),
                kind: 'speech',
              }, ...prev]);
              speak(phrase[langKey], localLang.code);
            }}
            className="
              p-4 bg-white border-2 border-blue-300 rounded-xl 
              text-left hover:border-blue-500 hover:shadow-lg
              active:scale-95 transition-all min-h-[80px]
            "
          >
            <div className="flex items-center gap-2 mb-2">
              <span className="text-2xl">{phrase.emoji}</span>
              <span className="font-bold text-lg">{phrase.ko}</span>
            </div>
            <div className="text-base text-gray-700">{phrase[langKey]}</div>
          </button>
        );
      })}
    </div>
  </div>
)}

□ 저장
□ 테스트: 빠른 문장 클릭 → 번역 → 음성
□ 커밋
```

**예상 소요**: 2시간

---

### ⏰ 오후 (2시간): 안내 메시지 강화

**체크리스트**:
```
□ 312-325줄 (헤더 부분) 수정
□ 언어 자동 선택 안내 추가:

<div className="bg-green-100 border-2 border-green-400 rounded-xl p-3 mb-4">
  <p className="text-lg font-bold text-green-800">
    🏝️ 오늘은 {destination} 방문!
  </p>
  <p className="text-base text-green-700">
    자동으로 {localLang.flag} {localLang.name} 번역이 준비되었어요.
  </p>
</div>

□ 저장
□ 테스트
```

**예상 소요**: 2시간

**Day 2 총 소요**: 6시간

---

## 📅 Day 3 (수요일) - 네비게이션 UI 개선

### ⏰ 전일 (8시간)

#### ✅ 작업 1: 연관검색 버튼 크기 증가 (3시간)

**파일**: 
- `/app/chat/components/handlers/SuggestChips.tsx` (있으면)
- 또는 NavigatePicker 관련 파일

**체크리스트**:
```
□ 연관검색 버튼 파일 찾기
□ 버튼 크기 증가:

// Before
className="px-3 py-1.5 rounded-full"

// After
className="
  px-5 py-4
  rounded-xl
  border-2
  text-lg font-bold
  min-h-[70px]
  bg-white
  hover:bg-blue-50
  hover:border-blue-500
  active:scale-95
  transition-all
  shadow-sm
"

// 내용도 강화
<div className="flex flex-col items-start gap-1">
  <span className="text-lg font-bold">{it.label}</span>
  {it.subtitle && (
    <span className="text-sm text-gray-600">{it.subtitle}</span>
  )}
</div>

□ 저장
□ 테스트: 검색 시 연관검색 버튼 표시
```

**예상 소요**: 3시간

---

#### ✅ 작업 2: 구글 맵 버튼 크기 증가 (3시간)

**파일**: 
- `/app/chat/components/blocks/NavigationBlock.tsx` (또는 유사 파일)
- 또는 구글 맵 링크를 표시하는 부분

**체크리스트**:
```
□ 구글 맵 링크 표시 부분 찾기
□ 3개 버튼 (대중교통, 자동차, 지도보기) 크기 증가:

<div className="grid grid-cols-3 gap-3">
  <a
    href={routes.transit}
    target="_blank"
    className="
      flex flex-col items-center justify-center gap-2
      px-4 py-5
      bg-gradient-to-br from-blue-500 to-blue-600
      text-white
      rounded-xl
      shadow-lg
      hover:shadow-xl
      active:scale-95
      transition-all
      min-h-[100px]
    "
  >
    <span className="text-4xl">🚌</span>
    <span className="text-lg font-bold">대중교통</span>
    <span className="text-xs opacity-90">버스/지하철</span>
  </a>
  
  <a
    href={routes.driving}
    target="_blank"
    className="... from-green-500 to-green-600 ..."
  >
    <span className="text-4xl">🚗</span>
    <span className="text-lg font-bold">자동차</span>
    <span className="text-xs opacity-90">택시/렌터카</span>
  </a>
  
  <a
    href={routes.mapview}
    target="_blank"
    className="... from-purple-500 to-purple-600 ..."
  >
    <span className="text-4xl">🗺️</span>
    <span className="text-lg font-bold">지도보기</span>
    <span className="text-xs opacity-90">위치 확인</span>
  </a>
</div>

□ 저장
□ 테스트: 검색 결과에 구글 맵 버튼 3개 표시
□ 각 버튼 클릭 시 구글 맵 열림 확인
```

**예상 소요**: 3시간

---

#### ✅ 작업 3: 안내 메시지 추가 (2시간)

**체크리스트**:
```
□ 입력창 위에 안내 메시지 추가:

<div className="mb-4 p-4 bg-blue-50 rounded-xl">
  <p className="text-lg">
    💡 <b>팁</b>: "현 위치"를 입력하면 지금 있는 곳에서 출발해요
  </p>
  <p className="text-base text-gray-700 mt-2">
    예시: "현 위치 → 후쿠오카 터미널"
  </p>
</div>

□ 저장
□ 테스트
```

**예상 소요**: 2시간

**Day 3 총 소요**: 8시간

---

## 📅 Day 4 (목요일) - 사진 검색 강화

### ⏰ 오전 (4시간): "보여줘" → 구글 이미지 검색

#### ✅ 작업 1: 의도 감지 로직 (2시간)

**파일**: `/lib/chat/detect.ts` (새 파일 또는 기존 파일)

**체크리스트**:
```
□ lib/chat/detect.ts 파일 생성 (또는 기존 파일 열기)
□ 다음 함수 추가:

export function detectShowMeIntent(text: string): boolean {
  const patterns = [
    /보여\s*줘/,
    /보여\s*주세요/,
    /사진\s*보여/,
    /이미지\s*보여/,
  ];
  return patterns.some(p => p.test(text));
}

export function extractShowMeQuery(text: string): string {
  return text
    .replace(/보여\s*(줘|주세요)/g, '')
    .replace(/사진/g, '')
    .replace(/이미지/g, '')
    .trim();
}

□ lib/nav/google.ts 파일 열기 (또는 생성)
□ 구글 이미지 검색 함수 추가:

export function googleImageSearch(query: string): string {
  return `https://www.google.com/search?q=${encodeURIComponent(query)}&tbm=isch`;
}

□ 저장
```

**예상 소요**: 2시간

---

#### ✅ 작업 2: 채팅 API에 통합 (2시간)

**파일**: `/app/api/chat/route.ts` (또는 핸들러 파일)

**체크리스트**:
```
□ /app/api/chat/route.ts 파일 열기
□ "보여줘" 감지 로직 추가:

import { detectShowMeIntent, extractShowMeQuery } from '@/lib/chat/detect';
import { googleImageSearch } from '@/lib/nav/google';

// POST 핸들러 내부
const userText = body.text;

if (detectShowMeIntent(userText)) {
  const query = extractShowMeQuery(userText);
  
  // 크루즈닷 자체 사진 검색
  const photosRes = await fetch(`${baseUrl}/api/photos?q=${query}`);
  const photosData = await photosRes.json();
  
  // 구글 이미지 검색 링크
  const googleUrl = googleImageSearch(query);
  
  return NextResponse.json({
    ok: true,
    messages: [
      {
        id: nanoid(),
        role: 'assistant',
        type: 'show-me',
        text: `${query} 사진을 찾았어요! 🎉`,
        query: query,
        googleImageUrl: googleUrl,
        cruisePhotos: photosData.items || [],
      }
    ],
  });
}

□ 저장
```

**예상 소요**: 2시간

---

### ⏰ 오후 (4시간): 사진 갤러리 UI 개선

#### ✅ 작업 1: "보여줘" UI 블록 (2시간)

**파일**: `/app/chat/components/ChatMessages.tsx` (또는 새 블록)

**체크리스트**:
```
□ ChatMessages 파일 찾기
□ 'show-me' 타입 처리 추가:

{message.type === 'show-me' && (
  <div className="space-y-4">
    <p className="text-xl font-bold">{message.text}</p>
    
    {/* 구글 이미지 검색 버튼 (크게!) */}
    <a
      href={message.googleImageUrl}
      target="_blank"
      className="
        flex items-center justify-center gap-3
        px-6 py-6
        bg-gradient-to-r from-red-500 to-pink-500
        text-white
        rounded-xl
        shadow-lg
        hover:shadow-xl
        text-xl font-bold
        min-h-[100px]
        active:scale-95
        transition-all
      "
    >
      <span className="text-4xl">🌐</span>
      <div className="flex flex-col items-start">
        <span>구글에서 더 많은 사진 보기</span>
        <span className="text-sm opacity-90">Google 이미지 검색</span>
      </div>
    </a>
    
    {/* 크루즈닷 자체 사진 */}
    {message.cruisePhotos?.length > 0 && (
      <>
        <div className="flex items-center gap-2 text-xl font-bold">
          <span>📸</span>
          <span>크루즈닷만의 사진 ({message.cruisePhotos.length}장)</span>
        </div>
        <PhotoAlbum 
          items={message.cruisePhotos} 
          query={message.query}
          onOpenAll={() => {/* 전체 보기 */}}
          onClickImage={(idx) => {/* 확대 보기 */}}
        />
      </>
    )}
  </div>
)}

□ 저장
□ 테스트: "후쿠오카 맛집 보여줘" 입력
```

**예상 소요**: 2시간

---

#### ✅ 작업 2: 하위 폴더 버튼 크기 증가 (2시간)

**파일**: `/app/chat/components/blocks/PhotoAlbum.tsx`

**체크리스트**:
```
□ 파일 열기: /app/chat/components/blocks/PhotoAlbum.tsx
□ 88-100줄 (rootOrder 버튼) 수정:

// Before
className="px-3 py-1 rounded-full"

// After
className="
  px-5 py-3
  rounded-xl
  border-2
  text-lg font-bold
  min-h-[60px]
  ${r === activeRoot 
    ? 'bg-red-600 text-white border-red-600 shadow-lg' 
    : 'bg-white text-gray-800 border-gray-300 hover:border-red-400'
  }
  active:scale-95
  transition-all
"

□ 104-113줄 (subs 버튼)도 동일하게 수정
□ 썸네일 크기 증가 (117-135줄):

// Before
grid grid-cols-3 gap-2

// After
grid grid-cols-2 md:grid-cols-3 gap-4

□ 저장
□ 테스트: 사진 검색 → 하위 폴더 버튼 표시
```

**예상 소요**: 2시간

**Day 4 총 소요**: 8시간

---

## 📅 Day 5 (금요일) - 튜토리얼 & 테스트

### ⏰ 오전 (4시간): 통합 튜토리얼

#### ✅ 작업 1: 번역기 튜토리얼 (2시간)

**파일**: `/app/translator/components/TranslatorTutorial.tsx` (새 파일)

**체크리스트**:
```
□ 폴더 생성: /app/translator/components/
□ TranslatorTutorial.tsx 파일 생성
□ 다음 코드 붙여넣기:

'use client';
import { useState } from 'react';

const STEPS = [
  {
    title: '사진을 찍으면 메뉴판을 번역해드려요!',
    description: '일본어, 중국어 메뉴판도 한글로 보세요.',
    emoji: '📷',
  },
  {
    title: '말하기 버튼을 꾹 누르고 말하세요',
    description: '한국어로 말하면 자동으로 번역됩니다.',
    emoji: '🎤',
  },
  {
    title: '빠른 문장을 클릭하면 바로 번역!',
    description: '자주 쓰는 말을 버튼으로 준비했어요.',
    emoji: '⚡',
  },
];

export default function TranslatorTutorial({ onComplete }) {
  const [currentStep, setCurrentStep] = useState(0);
  const step = STEPS[currentStep];
  
  return (
    <div className="fixed inset-0 bg-black/70 z-50 flex items-center justify-center p-4">
      <div className="bg-white rounded-3xl p-8 max-w-lg w-full">
        <div className="flex gap-2 mb-6">
          {STEPS.map((_, idx) => (
            <div key={idx} className={`h-2 flex-1 rounded-full ${
              idx <= currentStep ? 'bg-blue-500' : 'bg-gray-200'
            }`} />
          ))}
        </div>
        
        <div className="text-center mb-6">
          <span className="text-9xl">{step.emoji}</span>
        </div>
        
        <h2 className="text-3xl font-bold mb-4 text-center">
          {step.title}
        </h2>
        <p className="text-xl text-gray-600 mb-8 text-center leading-relaxed">
          {step.description}
        </p>
        
        <div className="flex gap-3">
          <button
            onClick={() => {
              onComplete();
              localStorage.setItem('hasSeenTranslatorTutorial', 'true');
            }}
            className="flex-1 px-6 py-4 border-2 border-gray-300 rounded-xl text-lg font-medium"
          >
            건너뛰기
          </button>
          <button
            onClick={() => {
              if (currentStep < STEPS.length - 1) {
                setCurrentStep(prev => prev + 1);
              } else {
                onComplete();
                localStorage.setItem('hasSeenTranslatorTutorial', 'true');
              }
            }}
            className="flex-1 px-6 py-4 bg-blue-500 text-white rounded-xl text-lg font-bold"
          >
            {currentStep < STEPS.length - 1 ? '다음 →' : '시작하기 🚀'}
          </button>
        </div>
      </div>
    </div>
  );
}

□ 저장
□ /app/translator/page.tsx에 통합:

import TranslatorTutorial from './components/TranslatorTutorial';

const [showTutorial, setShowTutorial] = useState(false);

useEffect(() => {
  const hasSeen = localStorage.getItem('hasSeenTranslatorTutorial');
  if (!hasSeen) setShowTutorial(true);
}, []);

return (
  <>
    {showTutorial && (
      <TranslatorTutorial onComplete={() => setShowTutorial(false)} />
    )}
    {/* 기존 UI */}
  </>
);

□ 저장
□ 테스트: 첫 방문 시 튜토리얼 표시
```

**예상 소요**: 2시간

---

#### ✅ 작업 2: 검색 가이드 추가 (2시간)

**파일**: 채팅 페이지 또는 검색 입력 부분

**체크리스트**:
```
□ 검색 입력창 위에 가이드 추가:

<div className="mb-4 p-4 bg-gradient-to-r from-blue-50 to-purple-50 rounded-xl border-2 border-blue-200">
  <p className="text-lg font-bold mb-2">🔍 길찾기 사용법</p>
  <div className="space-y-1 text-base text-gray-700">
    <p>• "인천공항 → 후쿠오카 터미널" - 경로 안내</p>
    <p>• "현 위치 → 스타벅스" - 주변 검색</p>
    <p>• "후쿠오카 맛집 보여줘" - 사진 검색</p>
  </div>
</div>

□ 저장
□ 테스트
```

**예상 소요**: 2시간

---

### ⏰ 오후 (4시간): 통합 테스트 & 문서화

#### ✅ 전체 기능 테스트

**테스트 체크리스트**:
```
□ 1. 온보딩
  □ 크루즈 선택 드롭다운 동작
  □ 3개 상품 표시
  □ 선택 → 자동 생성

□ 2. 번역기 (필수!)
  □ 버튼 높이 120px 확인
  □ 터치하기 쉬운지 확인
  □ 빠른 문장 8개 표시
  □ 빠른 문장 클릭 → 번역 → 음성
  □ 사진 찍기 → OCR → 번역 → 음성
  □ 음성 인식 → 번역 → 음성
  □ 언어 자동 선택 안내 표시
  □ 튜토리얼 표시 (첫 방문)

□ 3. 네비게이션
  □ "인천공항" 입력 → 연관검색 버튼
  □ 버튼 높이 70px 확인
  □ "현 위치 → 후쿠오카 터미널" 검색
  □ 구글 맵 버튼 3개 표시
  □ 버튼 높이 100px 확인
  □ 대중교통/자동차/지도 각각 클릭
  □ 구글 맵 정상 열림

□ 4. 사진 검색
  □ "후쿠오카 맛집 보여줘" 입력
  □ 구글 이미지 버튼 표시 (높이 100px)
  □ 크루즈닷 사진 표시
  □ 하위 폴더 버튼 60px 확인
  □ 사진 클릭 → 확대 보기
  □ 스와이프 동작

□ 5. 푸시 알림
  □ Proactive Engine 작동 중 확인
  □ 테스트 알림 발송
  □ 알림 수신 확인

□ 6. 50대+ 실사용 테스트 (중요!)
  □ 모든 버튼 터치하기 쉬운지
  □ 글씨 읽기 쉬운지
  □ 안내 메시지 이해하기 쉬운지
  □ 튜토리얼 도움 되는지
  □ 전체적으로 직관적인지

□ 7. 에러 상황 테스트
  □ 인터넷 끊김
  □ 마이크 권한 거부
  □ 카메라 권한 거부
  □ 환율 API 실패
```

**소요**: 4시간

---

## 📊 Week 1 완료 시 최종 상태

### ✅ 기능 완성도: 95%

| 기능 | Before | After | 완성도 |
|------|--------|-------|--------|
| 온보딩 | 예약번호 입력 | 크루즈 선택 | 100% |
| 번역기 | 작은 버튼 | 120px + 빠른 문장 | 100% |
| 네비게이션 | 작은 버튼 | 70-100px | 95% |
| 사진 검색 | 크루즈닷만 | 구글 이미지 추가 | 100% |
| 푸시 알림 | - | - | 100% |

### ✅ 시니어 친화도: 90%
- 모든 버튼 60px 이상 ✅
- 명확한 이모티콘 ✅
- 큰 글씨 ✅
- 안내 메시지 ✅
- 튜토리얼 ✅

### ✅ 핵심 기능
1. **번역기** - 100% (언어 장벽 제거)
2. **검색** - 95% (길찾기 쉽게)
3. **알림** - 100% (출항 놓침 0건)

---

## 🚀 출시 준비 완료!

### Week 1 완료 시
- **출시 가능**: 95%
- **50대 만족도**: 90%
- **핵심 가치** 모두 달성:
  1. ✅ 언어 장벽 제거 (번역기)
  2. ✅ 길찾기 쉽게 (검색)
  3. ✅ 안전 보장 (알림)

### 추가 개선 (Week 2-6)
- Week 2: 음성 입력 (채팅), 글씨 크기 조절
- Week 3: 고대비 모드
- Week 4-5: 데이터 마이그레이션
- Week 6: 최종 개선

---

## ✅ 매일 체크

### 아침 루틴 (30분)
- [ ] 오늘의 작업 확인
- [ ] 개발 서버 시작
- [ ] 이전 작업 테스트

### 작업 중
- [ ] 자주 저장하기
- [ ] 자주 테스트하기
- [ ] 막히면 즉시 질문

### 저녁 루틴 (30분)
- [ ] 오늘 완료한 작업 체크
- [ ] 커밋 & 푸시
- [ ] 내일 작업 준비

---

## 🎊 성공 기준

### Week 1 완료 = 출시 가능!

**필수 조건**:
- ✅ 번역기 버튼 120px
- ✅ 검색 버튼 70-100px
- ✅ 빠른 문장 8개
- ✅ 구글 이미지 연동
- ✅ 튜토리얼 2개

**50대+ 테스트**:
- ✅ 버튼 누르기 쉬움
- ✅ 안내 이해하기 쉬움
- ✅ 기능 찾기 쉬움

---

**작성자**: 크루즈 지니 팀  
**작성일**: 2025년 11월 2일  
**시작일**: 2025년 11월 4일 (월)  
**완료 예정**: 2025년 11월 8일 (금)

🚀 **지금 바로 시작하세요!** 🚀

---

## 💡 빠른 시작 가이드

### 지금 당장 할 수 있는 것 (1시간)

```bash
# 1. 파일 열기
/components/SimplifiedOnboarding.tsx

# 2. 예약번호 입력 부분 찾기 (Ctrl+F: "예약번호")

# 3. 크루즈 선택 드롭다운으로 변경

# 4. 테스트
npm run dev
# 브라우저에서 http://localhost:3030/onboarding

# 5. 작동 확인 → 커밋!
```

**막히는 부분이 있으면 즉시 물어보세요!** 💪

















